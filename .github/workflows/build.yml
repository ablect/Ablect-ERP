name: Build Ablect Business Suite
on:
  push:
    branches:
      - main
      - feat/modern-sales-pos
      - feat/modern-sales-pos-next
  pull_request:
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Install dependencies
        run: npm install
      - name: Build renderer and Electron bundle
        run: npm run build
