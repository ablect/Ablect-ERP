import { AnimatePresence, motion } from "framer-motion";
import { Check, MoreHorizontal, Search, ShieldCheck, UserPlus, Users, X } from "lucide-react";
import { useMemo, useState, type ReactNode } from "react";
import { useAdminStore, type AdminRole, type AppModule, type PermissionAction } from "../store/AdminStore";
import PageContainer from "../../../components/ui/PageContainer";
const roles: AdminRole[]=["Super Admin","Administrator","Sales Manager","Cashier","Inventory Manager","HR Manager","Finance","Marketer","Viewer"];
const actions: PermissionAction[]=["view","create","edit","delete","export"];
const modules: AppModule[]=["Dashboard","Sales","Products","Inventory","Purchases","Customers","Suppliers","Warehouse","CRM","HR","Payroll","Reports","Settings","Users"];
export default function UsersPage(){
 const store=useAdminStore(); const [tab,setTab]=useState<"users"|"roles">("users"); const [query,setQuery]=useState(""); const [selected,setSelected]=useState<string|null>(null); const [createOpen,setCreateOpen]=useState(false); const [form,setForm]=useState({name:"",email:"",department:"Sales",role:"Cashier" as AdminRole});
 const users=useMemo(()=>store.users.filter(u=>`${u.name} ${u.email} ${u.role} ${u.department}`.toLowerCase().includes(query.toLowerCase())),[store.users,query]); const selectedUser=store.users.find(u=>u.id===selected)??null; const matrix=store.roles[store.selectedRole];
 const create=()=>{if(!form.name.trim()||!form.email.trim())return;store.addUser({name:form.name.trim(),email:form.email.trim(),department:form.department,role:form.role,status:"Invited",twoFactor:false});store.addAuditLog({actor:"Current Admin",action:`Invited ${form.name}`,module:"Users",severity:"Info"});setForm({name:"",email:"",department:"Sales",role:"Cashier"});setCreateOpen(false)};
 return <PageContainer><div className="min-h-full space-y-6 bg-[#f8fafc] pb-14"><header className="rounded-[2rem] border border-slate-200 bg-white p-6 shadow-sm sm:p-8"><div className="flex flex-col gap-6 lg:flex-row lg:items-end lg:justify-between"><div><p className="text-xs font-black uppercase tracking-[0.22em] text-violet-600">Administration</p><h1 className="mt-2 text-3xl font-black tracking-tight text-slate-950">Users & access control</h1><p className="mt-2 max-w-3xl text-sm leading-6 text-slate-500">A role-driven security centre for who can sell, manage inventory, approve discounts, view reports and administer the ERP.</p></div><button onClick={()=>setCreateOpen(true)} className="inline-flex items-center justify-center gap-2 rounded-xl bg-violet-600 px-5 py-3 text-sm font-bold text-white shadow-lg shadow-violet-200 hover:bg-violet-700"><UserPlus size={17}/> Invite user</button></div><div className="mt-7 grid gap-4 sm:grid-cols-3"><Metric icon={<Users size={18}/>} label="System users" value={store.users.length} tone="violet"/><Metric icon={<ShieldCheck size={18}/>} label="2FA protected" value={store.users.filter(u=>u.twoFactor).length} tone="emerald"/><Metric icon={<ShieldCheck size={18}/>} label="Roles" value={roles.length} tone="sky"/></div></header>
<div className="flex flex-wrap gap-2 rounded-2xl border border-slate-200 bg-white p-2 shadow-sm"><button onClick={()=>setTab("users")} className={`rounded-xl px-4 py-2.5 text-sm font-bold ${tab==="users"?"bg-slate-950 text-white":"text-slate-500 hover:bg-slate-100"}`}>User directory</button><button onClick={()=>setTab("roles")} className={`rounded-xl px-4 py-2.5 text-sm font-bold ${tab==="roles"?"bg-slate-950 text-white":"text-slate-500 hover:bg-slate-100"}`}>RBAC matrix</button></div>
{tab==="users"?<section className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="flex flex-col gap-4 border-b border-slate-200 p-5 md:flex-row md:items-center md:justify-between"><div><h2 className="font-black text-slate-950">People with system access</h2><p className="mt-1 text-xs text-slate-500">Every account inherits a role and its permission policy.</p></div><div className="flex w-full items-center gap-3 rounded-xl border border-slate-200 px-4 py-2.5 md:max-w-sm"><Search size={17} className="text-slate-400"/><input value={query} onChange={e=>setQuery(e.target.value)} className="min-w-0 flex-1 bg-transparent text-sm outline-none" placeholder="Search users..."/></div></div><div className="overflow-x-auto"><table className="min-w-[900px] w-full"><thead className="bg-slate-50"><tr>{["User","Department","Role","Security","Last active","Status",""].map(h=><th key={h} className="px-5 py-4 text-left text-[10px] font-black uppercase tracking-wider text-slate-400">{h}</th>)}</tr></thead><tbody>{users.map(user=><motion.tr layout whileHover={{backgroundColor:"#f8fafc"}} key={user.id} onClick={()=>setSelected(user.id)} className="cursor-pointer border-t border-slate-100"><td className="px-5 py-4"><div className="flex items-center gap-3"><div className="grid h-10 w-10 place-items-center rounded-xl bg-violet-100 font-black text-violet-700">{user.name.split(" ").map(x=>x[0]).slice(0,2).join("")}</div><div><p className="text-sm font-bold text-slate-900">{user.name}</p><p className="text-xs text-slate-500">{user.email}</p></div></div></td><td className="px-5 py-4 text-sm font-semibold text-slate-600">{user.department}</td><td className="px-5 py-4"><span className="rounded-full bg-sky-50 px-3 py-1.5 text-xs font-bold text-sky-700">{user.role}</span></td><td className="px-5 py-4 text-xs font-bold">{user.twoFactor?<span className="text-emerald-600">2FA enabled</span>:<span className="text-amber-600">2FA off</span>}</td><td className="px-5 py-4 text-xs text-slate-500">{user.lastActive}</td><td className="px-5 py-4"><span className={`rounded-full px-3 py-1.5 text-xs font-bold ${user.status==="Active"?"bg-emerald-50 text-emerald-700":user.status==="Suspended"?"bg-rose-50 text-rose-700":"bg-sky-50 text-sky-700"}`}>{user.status}</span></td><td className="px-5 py-4"><MoreHorizontal size={18} className="text-slate-400"/></td></motion.tr>)}</tbody></table></div></section>:<section className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="grid gap-6 p-5 lg:grid-cols-[230px_1fr]"><aside className="space-y-2"><p className="px-3 pb-2 text-[10px] font-black uppercase tracking-wider text-slate-400">Roles</p>{roles.map(role=><button key={role} onClick={()=>store.setSelectedRole(role)} className={`w-full rounded-xl px-3 py-3 text-left text-sm font-bold ${store.selectedRole===role?"bg-violet-50 text-violet-700":"text-slate-600 hover:bg-slate-50"}`}>{role}</button>)}</aside><div className="min-w-0"><div className="mb-5"><h2 className="text-xl font-black text-slate-950">{store.selectedRole} permissions</h2><p className="text-xs text-slate-500">View, create, edit, delete and export are independently controlled.</p></div><div className="overflow-auto rounded-xl border border-slate-200"><table className="min-w-[960px] w-full"><thead className="bg-slate-50"><tr><th className="px-4 py-3 text-left text-[10px] font-black uppercase tracking-wider text-slate-400">Module</th>{actions.map(a=><th key={a} className="px-4 py-3 text-center text-[10px] font-black uppercase tracking-wider text-slate-400">{a}</th>)}</tr></thead><tbody>{modules.map(module=><tr key={module} className="border-t border-slate-100"><td className="px-4 py-3 text-sm font-bold text-slate-700">{module}</td>{actions.map(action=><td key={action} className="px-4 py-3 text-center"><button onClick={()=>store.togglePermission(store.selectedRole,module,action)} className={`mx-auto grid h-8 w-8 place-items-center rounded-lg ${matrix[module][action]?"bg-emerald-100 text-emerald-700":"bg-slate-100 text-slate-300"}`}>{matrix[module][action]&&<Check size={15}/>}</button></td>)}</tr>)}</tbody></table></div></div></div></section>}
<AnimatePresence>{selectedUser&&<><motion.button initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}} onClick={()=>setSelected(null)} className="fixed inset-0 z-40 bg-slate-950/20 backdrop-blur-sm"/><motion.aside initial={{x:"100%"}} animate={{x:0}} exit={{x:"100%"}} transition={{type:"spring",stiffness:280,damping:28}} className="fixed right-0 top-0 z-50 h-full w-full max-w-xl overflow-y-auto border-l border-slate-200 bg-white shadow-2xl"><div className="flex items-start justify-between border-b border-slate-200 p-6"><div><p className="text-xs font-black uppercase tracking-wider text-violet-600">User profile</p><h2 className="mt-1 text-2xl font-black text-slate-950">{selectedUser.name}</h2><p className="mt-1 text-sm text-slate-500">{selectedUser.email}</p></div><button onClick={()=>setSelected(null)} className="rounded-xl p-2 text-slate-400 hover:bg-slate-100"><X size={20}/></button></div><div className="space-y-5 p-6"><FieldSelect label="Role" value={selectedUser.role} options={roles} onChange={role=>{store.setUserRole(selectedUser.id,role as AdminRole);store.addAuditLog({actor:"Current Admin",action:`Changed ${selectedUser.name} role to ${role}`,module:"Users",severity:"Warning"})}}/><FieldSelect label="Account status" value={selectedUser.status} options={["Active","Suspended","Invited"]} onChange={status=>store.setUserStatus(selectedUser.id,status as typeof selectedUser.status)}/><div className="rounded-2xl bg-violet-50 p-5"><p className="text-xs font-black uppercase tracking-wider text-violet-600">Permission policy</p><p className="mt-2 text-sm leading-6 text-violet-950">This user inherits the {selectedUser.role} role. Edit the RBAC matrix to change what the role can access.</p></div></div></motion.aside></>}</AnimatePresence>
<AnimatePresence>{createOpen&&<div className="fixed inset-0 z-[60] grid place-items-center bg-slate-950/30 p-4 backdrop-blur-sm"><motion.div initial={{opacity:0,y:12}} animate={{opacity:1,y:0}} exit={{opacity:0,y:12}} className="w-full max-w-xl rounded-[2rem] border border-slate-200 bg-white p-6 shadow-2xl"><div className="flex items-start justify-between"><div><p className="text-xs font-black uppercase tracking-wider text-violet-600">New access</p><h2 className="mt-1 text-2xl font-black text-slate-950">Invite a user</h2></div><button onClick={()=>setCreateOpen(false)} className="rounded-xl p-2 text-slate-400 hover:bg-slate-100"><X size={20}/></button></div><div className="mt-6 grid gap-4 sm:grid-cols-2"><Field label="Full name" value={form.name} onChange={v=>setForm({...form,name:v})}/><Field label="Email" value={form.email} onChange={v=>setForm({...form,email:v})}/><Field label="Department" value={form.department} onChange={v=>setForm({...form,department:v})}/><FieldSelect label="Role" value={form.role} options={roles} onChange={v=>setForm({...form,role:v as AdminRole})}/></div><div className="mt-7 flex justify-end gap-3"><button onClick={()=>setCreateOpen(false)} className="rounded-xl border border-slate-200 px-5 py-3 text-sm font-bold text-slate-700">Cancel</button><button onClick={create} className="rounded-xl bg-violet-600 px-5 py-3 text-sm font-bold text-white hover:bg-violet-700">Create invitation</button></div></motion.div></div>}</AnimatePresence></div></PageContainer>;
}
function Field({label,value,onChange}:{label:string;value:string;onChange:(v:string)=>void}){return <label><span className="mb-2 block text-xs font-black uppercase tracking-wider text-slate-400">{label}</span><input value={value} onChange={e=>onChange(e.target.value)} className="w-full rounded-xl border border-slate-200 px-4 py-3 text-sm outline-none focus:border-violet-400 focus:ring-4 focus:ring-violet-50"/></label>}
function FieldSelect({label,value,options,onChange}:{label:string;value:string;options:readonly string[];onChange:(v:string)=>void}){return <label><span className="mb-2 block text-xs font-black uppercase tracking-wider text-slate-400">{label}</span><select value={value} onChange={e=>onChange(e.target.value)} className="w-full rounded-xl border border-slate-200 bg-white px-4 py-3 text-sm font-semibold">{options.map(o=><option key={o}>{o}</option>)}</select></label>}
function Metric({icon,label,value,tone}:{icon:ReactNode;label:string;value:number;tone:"violet"|"emerald"|"sky"}){const c={violet:"bg-violet-50 text-violet-700",emerald:"bg-emerald-50 text-emerald-700",sky:"bg-sky-50 text-sky-700"}[tone];return <div className={`rounded-2xl p-4 ${c}`}><div>{icon}</div><p className="mt-3 text-xs font-black uppercase tracking-wider opacity-70">{label}</p><p className="mt-1 text-2xl font-black">{value}</p></div>}
