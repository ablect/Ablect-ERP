import CustomerDashboard from "../components/customers/CustomerDashboard";

/** Route-level wrapper for the unified Customer 360 module. */
export default function Customers() {
  return <CustomerDashboard />;
}
