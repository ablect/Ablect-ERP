import { useEffect, useState, type FormEvent } from "react";
import { Navigate, useLocation, useNavigate } from "react-router-dom";
import { ArrowRight, Eye, EyeOff, LockKeyhole, ShieldCheck, Sparkles } from "lucide-react";
import { useAuth } from "../auth/AuthContext";
import { useClientConfig } from "../hooks/useClientConfig";
import "./LoginPage.css";
export default function LoginPage(){const{isAuthenticated,login}=useAuth();const navigate=useNavigate();const location=useLocation();const{config}=useClientConfig();const[identifier,setIdentifier]=useState("");const[password,setPassword]=useState("");const[showPassword,setShowPassword]=useState(false);const[loading,setLoading]=useState(false);const[error,setError]=useState("");const[databaseConnected,setDatabaseConnected]=useState<boolean|null>(null);useEffect(()=>{if(!window.ablectDesktop?.getDatabaseStatus)return;void window.ablectDesktop.getDatabaseStatus().then((status:any)=>setDatabaseConnected(status.connected));},[]);if(isAuthenticated)return <Navigate to="/" replace/>;async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setError("");setLoading(true);try{await login(identifier,password);const from=(location.state as {from?:string}|null)?.from??"/";navigate(from,{replace:true});}catch(err){setError(err instanceof Error?err.message:"Unable to sign in.");}finally{setLoading(false);}}return <main className="login-shell"><div className="login-orb login-orb-one"/><div className="login-orb login-orb-two"/><section className="login-panel"><div className="login-brand"><div className="login-brand-mark">{config.logoDataUrl?<img src={config.logoDataUrl} alt="" width={34} height={34}/>:<Sparkles size={22}/>}</div><div><p>ABLECT</p><span>Business Suite</span></div></div><div className="login-content"><div className="login-intro"><div className="login-badge"><ShieldCheck size={15}/> Secure local workspace</div><h1>Welcome to {config.businessName}.</h1><p>Sign in to manage your business from one intelligent local workspace.</p></div><form className="login-form" onSubmit={submit}><label><span>Username or email</span><div className="login-input-wrap"><input autoComplete="username" value={identifier} onChange={(event)=>setIdentifier(event.target.value)} placeholder="admin@ablect.local" disabled={loading}/></div></label><label><span>Password</span><div className="login-input-wrap"><LockKeyhole size={18}/><input type={showPassword?"text":"password"} autoComplete="current-password" value={password} onChange={(event)=>setPassword(event.target.value)} placeholder="Enter your password" disabled={loading}/><button type="button" className="password-toggle" onClick={()=>setShowPassword((value)=>!value)} aria-label={showPassword?"Hide password":"Show password"}>{showPassword?<EyeOff size={18}/>:<Eye size={18}/>}</button></div></label>{error&&<div className="login-error">{error}</div>}<button className="login-submit" type="submit" disabled={loading}><span>{loading?"Signing in…":"Sign in to workspace"}</span><ArrowRight size={19}/></button></form><div className="login-demo-note"><strong>Local installation</strong><span>Database {databaseConnected===true?"connected":databaseConnected===false?"not connected":"checking…"}</span></div></div><footer className="login-footer"><span>ABLECT Business Suite ERP</span><span>Powered By Able Business Suite · ABLECT TECHNOLOGIES LTD</span></footer></section></main>;}
