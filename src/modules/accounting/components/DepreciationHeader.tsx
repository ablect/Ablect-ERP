import SectionTitle
from "../../../components/ui/SectionTitle";

export default function DepreciationHeader(){

return(

<SectionTitle

title="Depreciation Register"

description="Generate, review and post depreciation for fixed assets."

/>

);

}